#!/usr/bin/python

'''show how shebangs, chmod, and dot-slash works.
Show what sys.version does (via command line).
Show that there are multiple versions of python installed in /usr/bin/.
Show that running the file from the interpreter explicitly runs it in python2.
Then chmod a+x <filename> each one. (all users, plus, execute)
Then run with dot-slash instead of explicitly through interpreter.
'''
import sys
python_version = sys.version
print("This file is through python %s." % python_version)
